h1 = @venue.name

h2 Venue

dl
  dt Foursquare ID 
  dd = @venue.foursquare_id
  dt Untappd ID
  dd = @venue.untappd_id

h2 Venue location (from Foursquare)

dl
  dt Coordinates
  dd = "#{@venue.location.latitude}, #{@venue.location.longitude}"
  dt Address
  dd
    address
      = @venue.location.street_address
      br
      = @venue.location.city
      br
      = @venue.location.postal_code
