window.app = window.app || {};

window.app.mapbox = {
  mapOptions: {
    scrollWheelZoom: false
  },
  mapboxURL: "<%= ENV['MAPBOX_URL'] %>",
  projectID: "<%= ENV['MAPBOX_PROJECT'] %>",
  accessToken: "<%= ENV['MAPBOX_TOKEN'] %>"
};

window.app.view = {};
