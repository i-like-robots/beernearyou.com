window.app = window.app || {};

window.app.mapbox = {
  mapOptions: {
    scrollWheelZoom: false
  },
  mapboxURL: "<%= APP_CONFIG['mapbox_api_url'] %>",
  projectID: "<%= APP_CONFIG['mapbox_project'] %>",
  accessToken: "<%= APP_CONFIG['mapbox_token'] %>"
};

window.app.geolocation = {
  enableHighAccuracy: true,
  maximumAge: 10000,
  timeout: 10000
};

window.app.view = {};
